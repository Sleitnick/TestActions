name: Selene Lint

on:
  pull_request:
    branches:
      master

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install Selene
      uses: actions-rs/install@v0.1
      with:
        crate: selene
        version: "0.6.0"
        use-tool-cache: true
    - name: Lint
      working-directory: ./src
      run: |
        selene .